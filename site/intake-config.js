/* Set only after private storage, owner access and an end-to-end receipt test are verified. */
(function(root){'use strict';root.ProtonIntakeConfig=Object.freeze({endpoint:'',schema:1});})(typeof window==='undefined'?globalThis:window);
